name: ci
on:
  pull_request:
    branches:
      - master
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Lint
        run: npm Lint
  tests:
    needs: check
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3

      - name: NPM install
        uses: bahmutov/npm-install@v1

      - name: Test 🧪
        run: npm test
